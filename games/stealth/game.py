"""Pygame stealth level: world, player, guards, vision cones, pathing, and the one
player verb that matters (throw). Local only — no networking beyond the judgment
calls. See BUILD.md section 6.

Build step 3: get this playable with rule-based guards first.
Build step 4: swap guard decisions to judge.choice (async, off the frame loop).
"""


def main() -> None:
    raise NotImplementedError("Build step 3: playable level with rule-based guards.")


if __name__ == "__main__":
    main()
